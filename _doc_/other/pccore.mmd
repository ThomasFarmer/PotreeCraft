
classDiagram
    class _ {
        +prepare_vectors_folder(source_vector_dir, output_dir)
        +resolve_pointcloud_name(output_dir, requested_name)
        +copy_plugin_jslibs(output_dir, log_callback)
        +compile_potree_project(...)
    }
